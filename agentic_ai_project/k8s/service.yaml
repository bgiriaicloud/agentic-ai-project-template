apiVersion: v1
kind: Service
metadata:
  name: agentic-ai-service
spec:
  type: LoadBalancer  # Or ClusterIP if using Ingress
  selector:
    app: agentic-ai
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
